/**
 * Static product metadata for Smart API.
 * Keeping these in one place makes renaming or versioning the service trivial.
 */
export const PRODUCT_NAME = 'Smart API'

export const MODEL_NAME = 'smart-api-playwright-rules-v1'

export const TAGLINE =
  'Smart API turns plain-English QA steps into clean, reliable Playwright TypeScript automation using a specialized rules engine.'

export const API_PREFIX = '/api/v1'
