PORT=4000
NODE_ENV=development

# API-key auth. Comma-separated list of accepted keys.
# Leave empty for open mode (local dev). Set in production to require a key,
# which callers send in the "x-api-key" header.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEYS=
